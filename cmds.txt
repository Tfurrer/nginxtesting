
#build
docker compose up -d

#execute
docker attach clientmac
ip addr show

ping -c 2 google.com
ping -c 2 172.21.0.2
ping -c 2 clientmac2

curl handler1:80 #Restricted
curl handler1:81 #Allowed

docker exec -it handler1 sh -c "echo 'allow 172.21.0.2;' > '/var/www-allow/board_a_allow.conf' && service nginx reload"
docker exec -it handler1 sh -c "echo 'deny all;' > '/var/www-allow/board_a_allow.conf' && service nginx reload"

#handler direct updates
docker exec -it handler1 sh
cd /var/www-allow/
cat board_a_allow.conf
echo "allow 172.21.0.2;" > 'board_a_allow.conf'

#Netowrk Inspections
docker network inspect clientsite
docker network inspect boardfarm